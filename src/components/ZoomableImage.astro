---
// Typed wrapper around the [data-zoomable] contract. From .astro:
//   <ZoomableImage src="/cool.jpg" alt="Cool" width={3943} height={2958} />
// From markdown (raw HTML, no Astro JSX available):
//   <img src="/cool.jpg" alt="Cool" data-zoomable width="3943" height="2958" loading="lazy">
// Both render an <img data-zoomable>; Default.astro owns the singleton handler.

interface Props {
  src: string;
  alt?: string;
  width?: number;
  height?: number;
  /** Caption text shown above the lightbox controls. */
  title?: string;
  /** Secondary metadata line (e.g. camera, date). */
  meta?: string;
  /** Higher-res source the lightbox upgrades to once the thumb has opened. */
  fullSrc?: string;
  loading?: 'eager' | 'lazy';
  class?: string;
}

const {
  src,
  alt = '',
  width,
  height,
  title,
  meta,
  fullSrc,
  loading = 'lazy',
  class: className,
} = Astro.props;
---
<img
  src={src}
  alt={alt}
  data-zoomable
  width={width}
  height={height}
  loading={loading}
  class={className}
  data-title={title}
  data-meta={meta}
  data-full-src={fullSrc}
/>
